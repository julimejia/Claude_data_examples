# ADR-0005 - architect decision in task:T-022

- Status: accepted
- Date: 2026-09-20
- Raised during: task:T-022
- Decided by: project owner via Telegram

## Context
Option A is recorded in ADR-0004 with a full implementation contract, but it only works if the dev role may persist files under scripts/ — a directory that does not exist yet, and whose writability is set by the devloop harness's per-role revert allowlist, which lives outside this repo (I confirmed the blocker is that revert, not a stash or .gitignore: the stash list is empty, .gitignore matches neither tools/ nor scripts/, and gitops.py commits with `git add -A`). Three ways forward: (1) add scripts/ to the dev role's write allowlist — recommended, because the tracker already specifies scripts/check_demo_size.py for T-023 and scripts/record_replays.py for T-028, so without it three tasks fail identically, and it keeps the dev out of tools/ where its own supervising harness lives; (2) you hand-commit scripts/gen_demo_examples.py from the ADR-0004 contract and let the dev only fix the test — unblocks T-022 this week but leaves T-023 and T-028 needing the same manual step twice more; (3) fall back to option C, delete the freshness test and rely on the two content tests that already prove every example maps to a golden case and matches the deterministic classification — cheapest, but T-022's 'generated by a script' AC stays only partly met. If you pick (1), confirm it and the dev can retry T-022 immediately against ADR-0004.

## Decision
3
